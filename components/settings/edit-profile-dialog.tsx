"use client"

/**
 * Re-export the profile Edit Profile dialog so settings code can
 * import { EditProfileDialog } from '@/components/settings/edit-profile-dialog'
 */
export { default as EditProfileDialog } from "../profile/edit-profile-dialog"
